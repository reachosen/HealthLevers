# Optional: Database connection (not required for AI Review Workbench demo)
# DATABASE_URL=postgresql://user:password@localhost:5432/healthlevers

# Optional: OpenAI API key (required only for AI-powered features)
# OPENAI_API_KEY=sk-...

# Optional: Session secret (will auto-generate if not set)
# SESSION_SECRET=your-secret-here

# Development mode
NODE_ENV=development
